{
  "name": "solana-citizen-platform",
  "version": "1.0.0",
  "description": "National Tokenization Hub - Blockchain-powered citizen platform for Kazakhstan",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:anchor\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:anchor": "cd blockchain && anchor localnet",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "setup": "npm install && cd blockchain && npm install && cd ../backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "blockchain",
    "backend", 
    "frontend"
  ]
}